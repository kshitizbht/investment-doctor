Read CLAUDE.md first

Since our focus is on optimizing for this year 2026. Update the Mock data accordingly. We will use estimated income from last year data.

/compact
In the calculator tab,
Let's build a Ask Claude field which will get this entire snapshot and suggest ways users can reduce tax liability for the upcoming year, or based on user situations it can provide more guidance. 
Provide options on how we can do it ex: do we need api key from claude to do this, if so how we will protect this api key or how can user login with their account but we build the prompt and use user account to query claude.




Update the CLAUDE.md with above features for next session after i clear context.


